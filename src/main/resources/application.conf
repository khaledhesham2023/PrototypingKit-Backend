ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [
      com.khaledamin.plugins.SerializationKt.configureSerialization
      com.khaledamin.plugins.RoutingKt.configureRouting
      com.khaledamin.plugins.DatabaseKt.configureDatabase
    ]
  }
}